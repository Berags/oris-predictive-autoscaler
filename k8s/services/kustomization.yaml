apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: oris-predictive-autoscaler

resources:
  - python-service
  - sirio-controller
  - inter-arrival-collector

commonLabels:
  app.kubernetes.io/part-of: services
